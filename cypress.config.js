module.exports = {
  e2e: {
    /**
     * Sets up the node event listeners for Cypress.
     * @param {string} on The event handler to listen to events.
     * @param {object} config The Cypress configuration object.
     */
    setupNodeEvents(on, config) {
      // implement node event listeners here
    },
  },
};
